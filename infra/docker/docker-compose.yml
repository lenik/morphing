services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: morphing
      POSTGRES_PASSWORD: morphing
      POSTGRES_DB: morphing
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
